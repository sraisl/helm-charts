apiVersion: v1
kind: Service
metadata:
  name: {{ include "faker-api.fullname" . }}
  labels:
    {{- include "faker-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "faker-api.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
